@model ShoppingCartVm

@if (Model.ShoppingCart.ShoppingCartItemsList.Count > 0)
{
    <li>
        <a asp-controller="ShoppingCart" asp-action="GetItems">
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @Model.ShoppingCart.ShoppingCartItemsList.Count
            </span>
        </a>
    </li>
}